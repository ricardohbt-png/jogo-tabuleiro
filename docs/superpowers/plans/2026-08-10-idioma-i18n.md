# Opção de Idioma + motor de tradução (PT/EN) — Plano de Implementação

> **Para agentes:** SUB-SKILL OBRIGATÓRIA: use `superpowers:subagent-driven-development` (recomendado) ou `superpowers:executing-plans` para executar tarefa a tarefa. Os passos usam caixas (`- [ ]`) para acompanhamento.

**Spec:** `docs/superpowers/specs/2026-08-10-idioma-i18n-design.md`

**Objetivo:** Adicionar uma opção **🌐 Idioma** no painel ⚙️ que troca o idioma do jogo entre português e inglês, junto do motor de tradução dos dois lados (cliente e servidor) e uma amostra traduzida ponta a ponta.

**Arquitetura:** Um dicionário único em `src/lang/strings.js` (`{chave: {pt, en}}`) lido pelo cliente via `<script>` e pelo servidor via `json.loads`. No servidor, um objeto `T("chave", **params)` marca "texto ainda não traduzido"; `broadcast()` e `send_to()` o resolvem no idioma de cada conexão através do parâmetro `default` do `json.dumps`, agrupando por idioma. No cliente, `src/i18n.js` é motor puro e `game.js` aplica no DOM via atributos `data-i18n`. String não migrada continua saindo em português — a migração em massa (etapa 2) é incremental e nunca deixa o jogo quebrado.

**Stack:** Python 3 + `websockets` (servidor), JavaScript vanilla sem bundler (cliente), testes `tools/test_*.py` (Python) e `tools/test_*.js` (node).

---

## Contexto que o executor precisa saber

Leia antes de começar:

- **`CLAUDE.md` na raiz** — regras de arquitetura obrigatórias. As duas que importam aqui: `src/gameState.js` **nunca** pode referenciar `document`/`canvas`/`THREE`; `game.js` é o único lugar de renderização. `src/i18n.js` é um módulo novo, no mesmo estilo de `src/visualConfig.js` e `src/difficulty.js` (que expõem `window.VC` e `window.Difficulty`) — pode usar `window`, mas **não** pode tocar em `document`.
- **Nunca edite `game.js` por PowerShell/`sed`.** Use a ferramenta Edit. O arquivo tem 25.810 linhas e todo o HTML do jogo dentro de uma template string.
- **Não use `git add <arquivo>` sem conferir `git status` antes.** O usuário edita `server.py` e `game.js` em paralelo; um `git add` largo varre trabalho dele para dentro do commit. Cada passo de commit abaixo lista os arquivos exatos.
- Rodar o servidor: `python server.py` (serve página + WebSocket na porta 8765). O usuário costuma ter a porta ocupada com o servidor dele — não presuma que 8765 está livre.

### Mapa de arquivos

| Arquivo | O que muda |
|---|---|
| `src/lang/strings.js` | **Criar.** O dicionário: uma chave por texto, com `pt` e `en`. |
| `src/i18n.js` | **Criar.** Motor puro do cliente: `I18N.t()`, `I18N.setLang()`, `I18N.on()`. Zero DOM. |
| `server.py` | Bloco de i18n novo (loader, `t`, `T`, `LANG_BY_PID`) após `BASE_DIR`; `broadcast`/`send_to`/`err` passam a traduzir; despacho de `set_lang`; limpeza no `finally`; 2 frases da amostra migradas. |
| `index.html` | Carrega `src/lang/strings.js` e `src/i18n.js` antes de `game.js`. |
| `game.js` | `_i18nApply()`, o seletor 🌐 no painel ⚙️, persistência `lfh_lang`, e `data-i18n` na amostra de HTML. |
| `src/gameState.js` | `setLang()` + envio de `set_lang` nos dois `ws.onopen`. |
| `tools/test_idioma.py` | **Criar.** Testes do servidor. |
| `tools/test_idioma_cliente.js` | **Criar.** Testes do cliente (node). |

---

## Task 1: Motor de tradução do servidor

**Arquivos:**
- Criar: `src/lang/strings.js`
- Criar: `tools/test_idioma.py`
- Modificar: `server.py` (inserir bloco novo após a linha `BASE_DIR = os.path.dirname(...)`, hoje na linha 1136)

- [ ] **Passo 1: Criar o dicionário com as duas chaves da amostra do servidor**

Criar `src/lang/strings.js` com exatamente este conteúdo:

```js
// Dicionário único do jogo — lido pelo CLIENTE via <script> (window.LANG_STRINGS)
// e pelo SERVIDOR via json.loads do objeto abaixo (server.py, _load_lang).
//
// REGRAS DO ARQUIVO:
//   • É JSON estrito dentro das chaves: aspas duplas, sem vírgula sobrando,
//     sem comentários DENTRO do objeto (este cabeçalho pode ter comentários,
//     desde que não contenha { nem }).
//   • Parâmetros são {nome} e são substituídos por nome, nunca por posição.
//   • Falta a chave "en"? Cai no "pt". É isso que permite traduzir em lotes.
window.LANG_STRINGS = {
  "narracao.abre_porta": {
    "pt": "🚪 **{nome}** abre uma porta!",
    "en": "🚪 **{nome}** opens a door!"
  },
  "erro.porta_longe": {
    "pt": "Aproxime-se da porta para abri-la.",
    "en": "Get closer to the door to open it."
  }
};
```

- [ ] **Passo 2: Escrever o teste que falha**

Criar `tools/test_idioma.py`:

```python
"""Motor de idioma (PT/EN) — dicionário único, objeto T e tradução por conexão.
Roda da raiz: python tools/test_idioma.py"""
import asyncio, json, os, re, sys
try: sys.stdout.reconfigure(encoding="utf-8")
except Exception: pass
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import server as S

PASS = 0; FAIL = 0
def check(name, cond):
    global PASS, FAIL
    if cond: PASS += 1; print(f"  ✅ {name}")
    else:    FAIL += 1; print(f"  ❌ {name}")


def _rodar_verificacoes():
    print("\n[1] Carregamento do dicionário")
    check("LANG_STRINGS carregou do disco", isinstance(S.LANG_STRINGS, dict) and len(S.LANG_STRINGS) > 0)
    check("chave da amostra existe", "narracao.abre_porta" in S.LANG_STRINGS)
    check("chave tem pt e en",
          set(S.LANG_STRINGS["narracao.abre_porta"]) >= {"pt", "en"})
    # Dicionário ilegível não pode impedir o servidor de subir: o jogo segue
    # inteiro em português, que é exatamente o estado de hoje.
    original = S.LANG_FILE
    S.LANG_FILE = os.path.join(os.path.dirname(original), "_nao_existe_.js")
    try:
        vazio = S._load_lang()
        check("arquivo ausente devolve dicionário vazio em vez de estourar", vazio == {})
    finally:
        S.LANG_FILE = original
    check("recarregar do arquivo real volta a funcionar",
          "narracao.abre_porta" in S._load_lang())

    print("\n[2] t() — tradução, fallback e chave ausente")
    check("t() devolve português", S.t("erro.porta_longe", "pt") == "Aproxime-se da porta para abri-la.")
    check("t() devolve inglês", S.t("erro.porta_longe", "en") == "Get closer to the door to open it.")
    S.LANG_STRINGS["_teste.so_pt"] = {"pt": "só em português"}
    check("falta 'en' → cai no português", S.t("_teste.so_pt", "en") == "só em português")
    check("chave inexistente devolve a própria chave", S.t("_teste.nao_existe", "en") == "_teste.nao_existe")
    check("idioma desconhecido cai no português", S.t("erro.porta_longe", "xx") == "Aproxime-se da porta para abri-la.")
    S.LANG_STRINGS.pop("_teste.so_pt", None)

    print("\n[3] Parâmetros {nome}")
    check("substitui por nome (pt)",
          S.t("narracao.abre_porta", "pt", nome="Thorin") == "🚪 **Thorin** abre uma porta!")
    check("substitui por nome (en)",
          S.t("narracao.abre_porta", "en", nome="Thorin") == "🚪 **Thorin** opens a door!")
    check("parâmetro que falta fica visível e não estoura",
          S.t("narracao.abre_porta", "pt") == "🚪 **{nome}** abre uma porta!")

    print("\n[4] Objeto T")
    marcado = S.T("narracao.abre_porta", nome="Lyra")
    check("T guarda a chave", marcado.key == "narracao.abre_porta")
    check("T guarda os parâmetros", marcado.params == {"nome": "Lyra"})
    check("T rende no idioma pedido", S._t_render(marcado, "en") == "🚪 **Lyra** opens a door!")


if __name__ == "__main__":
    print("=" * 62); print("  TESTE — Motor de idioma (PT/EN)"); print("=" * 62)
    _rodar_verificacoes()
    print("\n" + "=" * 62)
    print(f"  {PASS} passaram, {FAIL} falharam")
    print("=" * 62)
    sys.exit(1 if FAIL else 0)
```

- [ ] **Passo 3: Rodar o teste e confirmar que falha**

```bash
python tools/test_idioma.py
```

Esperado: `AttributeError: module 'server' has no attribute 'LANG_STRINGS'` — ou seja, quebra já na seção [1].

- [ ] **Passo 4: Confirmar que `re` já está importado no server**

```bash
grep -nE "^import |^from " server.py | head -20
```

Esperado: uma linha de import contendo `re`. Se **não** houver, acrescente `import re` junto dos outros imports do topo.

- [ ] **Passo 5: Implementar o bloco de i18n no servidor**

Em `server.py`, logo **depois** da linha `BASE_DIR = os.path.dirname(os.path.abspath(__file__))` e **antes** de `def _now_iso():`, inserir:

```python
# ─── IDIOMA (i18n) ────────────────────────────────────────────────────────
# Dicionário único, compartilhado com o cliente: src/lang/strings.js guarda
# {chave: {pt, en}} e é lido pelos DOIS lados — impossível divergirem.
#
# Como usar: envolva a frase em T("chave", param=valor) no lugar da string.
# A tradução acontece só na saída (broadcast/send_to), no idioma de cada
# conexão. String crua continua string crua e sai em português para todos,
# o que permite migrar as frases em lotes sem quebrar nada no caminho.
LANG_FILE = os.path.join(BASE_DIR, "src", "lang", "strings.js")
LANG_DEFAULT = "pt"
LANG_SUPORTADOS = ("pt", "en")
LANG_BY_PID = {}          # pid -> "pt"|"en". Fora da sala de propósito: o pid vem
                          # de new_id() (contador global), então o idioma vale
                          # antes de entrar em qualquer sala e o Mestre — que sai
                          # de self.players no start_game — não fica de fora.

def _load_lang():
    """Lê src/lang/strings.js e devolve o dicionário. O arquivo é JS por causa
    do cliente (carrega por <script>, sem fetch); aqui pegamos o objeto entre a
    primeira { e a última } e fazemos json.loads. Mesmo padrão que
    tools/editor_catalog.js já usa. Falha nunca impede o servidor de subir."""
    try:
        with open(LANG_FILE, encoding="utf-8") as f:
            raw = f.read()
        return json.loads(raw[raw.index("{"):raw.rindex("}") + 1])
    except Exception as e:
        print(f"⚠️  i18n: não foi possível ler {LANG_FILE} ({type(e).__name__}: {e}) — "
              f"o jogo segue em português.")
        return {}

LANG_STRINGS = _load_lang()

_LANG_PARAM_RE = re.compile(r"\{(\w+)\}")

def t(key, lang=LANG_DEFAULT, **params):
    """Traduz uma chave. Sem tradução no idioma pedido → português. Sem a chave
    → devolve a própria chave (aparece na tela, mas nada quebra)."""
    entry = LANG_STRINGS.get(key)
    if not isinstance(entry, dict):
        return key
    text = entry.get(lang) or entry.get(LANG_DEFAULT) or key
    if params:
        # Substituição por nome (e não .format) para que uma chave sem o
        # parâmetro — ou uma chave { solta no texto — nunca levante exceção.
        text = _LANG_PARAM_RE.sub(lambda m: str(params.get(m.group(1), m.group(0))), text)
    return text

class T:
    """Texto ainda não traduzido: guarda a chave e os parâmetros, e só vira
    string na hora do envio, no idioma daquele jogador."""
    __slots__ = ("key", "params")

    def __init__(self, key, **params):
        self.key = key
        self.params = params

    def __repr__(self):
        return f"T({self.key!r}, {self.params!r})"

def _t_render(o, lang):
    """Usado como `default` do json.dumps: chamado para cada objeto que o json
    não sabe serializar. Resolve T no idioma da conexão; qualquer outra coisa
    continua sendo um erro de serialização de verdade."""
    if isinstance(o, T):
        return t(o.key, lang, **o.params)
    raise TypeError(f"Object of type {type(o).__name__} is not JSON serializable")

def _lang_de(pid):
    lang = LANG_BY_PID.get(pid, LANG_DEFAULT)
    return lang if lang in LANG_SUPORTADOS else LANG_DEFAULT
```

- [ ] **Passo 6: Rodar o teste e confirmar que passa**

```bash
python tools/test_idioma.py
```

Esperado: `16 passaram, 0 falharam`.

- [ ] **Passo 7: Commit**

```bash
git add src/lang/strings.js tools/test_idioma.py server.py
git commit -m "feat(i18n): dicionario unico e motor de traducao do servidor"
```

---

## Task 2: Traduzir na saída (`broadcast`, `send_to`, `err`)

**Arquivos:**
- Modificar: `server.py:6738-6760` (`broadcast` e `send_to`) e `server.py:25201-25202` (`err` do handler)
- Modificar: `tools/test_idioma.py`

- [ ] **Passo 1: Escrever o teste que falha**

Em `tools/test_idioma.py`, acrescentar a classe auxiliar logo **antes** de `def _rodar_verificacoes():`:

```python
class RecWS:
    """WebSocket falso que guarda o JSON cru enviado — é ele que prova que os
    dois jogadores receberam a MESMA mensagem em idiomas diferentes."""
    def __init__(self): self.sent = []
    async def send(self, data): self.sent.append(data)

def _sala_dois_idiomas():
    """Sala com dois jogadores conectados: p1 em português, p2 em inglês."""
    r = S.GameRoom("TEST")
    ws_pt, ws_en = RecWS(), RecWS()
    r.connections = {"pid_pt": ws_pt, "pid_en": ws_en}
    S.LANG_BY_PID["pid_pt"] = "pt"
    S.LANG_BY_PID["pid_en"] = "en"
    return r, ws_pt, ws_en

def _limpar_idiomas():
    for pid in ("pid_pt", "pid_en"):
        S.LANG_BY_PID.pop(pid, None)
```

E acrescentar ao fim de `_rodar_verificacoes()`:

```python
    print("\n[5] broadcast traduz por conexão")
    sala, ws_pt, ws_en = _sala_dois_idiomas()
    asyncio.run(sala.gm_say(S.T("narracao.abre_porta", nome="Thorin")))
    texto_pt = json.loads(ws_pt.sent[-1])["text"]
    texto_en = json.loads(ws_en.sent[-1])["text"]
    check("jogador em pt recebe português", texto_pt == "🚪 **Thorin** abre uma porta!")
    check("jogador em en recebe inglês",   texto_en == "🚪 **Thorin** opens a door!")
    check("a MESMA narração chegou nos dois idiomas", texto_pt != texto_en)

    print("\n[6] send_to traduz para um jogador só")
    asyncio.run(sala.send_to("pid_en", {"type": "error", "msg": S.T("erro.porta_longe")}))
    check("erro sai em inglês para quem está em inglês",
          json.loads(ws_en.sent[-1])["msg"] == "Get closer to the door to open it.")

    print("\n[7] Sem set_lang → português")
    sala2 = S.GameRoom("TEST2")
    ws_mudo = RecWS()
    sala2.connections = {"pid_sem_lang": ws_mudo}
    asyncio.run(sala2.gm_say(S.T("narracao.abre_porta", nome="Anon")))
    check("conexão que nunca mandou set_lang recebe português",
          json.loads(ws_mudo.sent[-1])["text"] == "🚪 **Anon** abre uma porta!")

    print("\n[8] String crua continua saindo igual")
    asyncio.run(sala.gm_say("texto legado sem chave"))
    check("string crua sai idêntica em pt", json.loads(ws_pt.sent[-1])["text"] == "texto legado sem chave")
    check("string crua sai idêntica em en", json.loads(ws_en.sent[-1])["text"] == "texto legado sem chave")
    _limpar_idiomas()
```

- [ ] **Passo 2: Rodar o teste e confirmar que falha**

```bash
python tools/test_idioma.py
```

Esperado: falha na seção [5] com `TypeError: Object of type T is not JSON serializable` — o `broadcast` ainda serializa sem o `default`.

- [ ] **Passo 3: Implementar a tradução na saída**

Em `server.py`, substituir os corpos de `broadcast` e `send_to` (linhas 6738-6760). O `broadcast` passa a agrupar por idioma — com todos no mesmo idioma, é um `json.dumps` só, igual a hoje:

```python
    async def broadcast(self, msg, skip=None):
        """`skip`: iterável de pids que NÃO devem receber esta mensagem —
        usado para não mandar game_state a quem está na cidade (fora_masmorra)."""
        dead = []
        skip = skip or ()
        por_idioma = {}   # lang -> JSON já serializado (um dumps por idioma na sala)
        for pid, ws in list(self.connections.items()):
            if pid in skip:
                continue
            lang = _lang_de(pid)
            data = por_idioma.get(lang)
            if data is None:
                data = por_idioma[lang] = json.dumps(
                    msg, default=lambda o, _l=lang: _t_render(o, _l))
            try:
                await ws.send(data)
            except Exception:
                dead.append(pid)
        for pid in dead:
            self.connections.pop(pid, None)

    async def send_to(self, pid, msg):
        ws = self.connections.get(pid)
        if ws:
            lang = _lang_de(pid)
            try:
                await ws.send(json.dumps(msg, default=lambda o: _t_render(o, lang)))
            except Exception:
                pass
```

Em `server.py`, dentro de `async def handler(ws)` (linha 25201), o `err` local também precisa traduzir — é ele que responde antes de o jogador entrar numa sala:

```python
    async def err(msg):
        await ws.send(json.dumps({"type": "error", "msg": msg},
                                 default=lambda o: _t_render(o, _lang_de(pid))))
```

- [ ] **Passo 4: Rodar o teste e confirmar que passa**

```bash
python tools/test_idioma.py
```

Esperado: `23 passaram, 0 falharam`.

- [ ] **Passo 5: Rodar a suíte vizinha para garantir que nada regrediu**

```bash
python tools/test_modo_mestre.py
```

Esperado: mesma contagem de antes da Task 1 (`0 falharam`). Se algo falhar aqui, **pare** — a mudança em `broadcast`/`send_to` toca o jogo inteiro.

- [ ] **Passo 6: Commit**

```bash
git add server.py tools/test_idioma.py
git commit -m "feat(i18n): broadcast e send_to traduzem no idioma de cada conexao"
```

---

## Task 3: Mensagem `set_lang`

**Arquivos:**
- Modificar: `server.py` (despacho perto da linha 25219; `finally` na linha 25943)
- Modificar: `tools/test_idioma.py`
- Modificar: `CLAUDE.md` (tabela Client → Server)

- [ ] **Passo 1: Escrever o teste que falha**

Acrescentar ao fim de `_rodar_verificacoes()` em `tools/test_idioma.py`:

```python
    print("\n[9] Validação do idioma recebido")
    check("idioma suportado é aceito", S._lang_valido("en") == "en")
    check("idioma desconhecido vira português", S._lang_valido("klingon") == "pt")
    check("valor não-string vira português", S._lang_valido(42) == "pt")
    check("None vira português", S._lang_valido(None) == "pt")
```

- [ ] **Passo 2: Rodar o teste e confirmar que falha**

```bash
python tools/test_idioma.py
```

Esperado: falha na seção [9] com `AttributeError: module 'server' has no attribute '_lang_valido'`.

- [ ] **Passo 3: Implementar o validador**

Em `server.py`, no bloco de i18n criado na Task 1, logo **depois** de `def _lang_de(pid):`, acrescentar:

```python
def _lang_valido(valor):
    """Normaliza o que veio do cliente. Qualquer coisa fora da lista vira o
    padrão — a mensagem vem da rede e não é confiável."""
    return valor if valor in LANG_SUPORTADOS else LANG_DEFAULT
```

- [ ] **Passo 4: Implementar o despacho de `set_lang`**

Em `server.py`, dentro de `async def handler(ws)`, **antes** do bloco `if t == "upload_story":` (linha 25219), inserir:

```python
                # Idioma desta conexão. Vem antes de qualquer sala: vale já na
                # tela de login e nas mensagens de erro do lobby.
                if t == "set_lang":
                    LANG_BY_PID[pid] = _lang_valido(msg.get("lang"))
                    # Reenvia o estado para que o log de narração já impresso
                    # reapareça traduzido, sem função de re-render nova no cliente.
                    if room:
                        if   room.phase == "playing": await room.push_state()
                        elif room.phase == "city":    await room.broadcast_city_state()
                        else:                         await room.broadcast_lobby()
                    continue
```

- [ ] **Passo 5: Limpar o idioma na desconexão**

Em `server.py`, no `finally:` do `handler` (linha 25943), acrescentar como **primeira** linha do bloco:

```python
        LANG_BY_PID.pop(pid, None)
```

Ficando:

```python
    finally:
        LANG_BY_PID.pop(pid, None)
        if account["name"] and ACCOUNTS_ONLINE.get(account["name"]) == pid:
            del ACCOUNTS_ONLINE[account["name"]]
```

- [ ] **Passo 6: Rodar o teste e confirmar que passa**

```bash
python tools/test_idioma.py
```

Esperado: `27 passaram, 0 falharam`.

- [ ] **Passo 7: Documentar a mensagem no CLAUDE.md**

Em `CLAUDE.md`, na tabela **Client → Server**, acrescentar uma linha ao fim (depois da linha de `disparar_fala`):

```markdown
| `set_lang` | `lang` (`"pt"`\|`"en"`) — idioma desta conexão. Enviada no `onopen` e a cada troca no painel ⚙️. Guardada em `LANG_BY_PID` (módulo, chaveada pelo `pid` do `new_id()`), não na sala — vale antes de entrar em qualquer sala e cobre o Mestre, que sai de `self.players` no `start_game`. O servidor reenvia o estado ao recebê-la, para o log de narração reaparecer traduzido. Valor fora da lista cai em `pt`. |
```

- [ ] **Passo 8: Commit**

```bash
git add server.py tools/test_idioma.py CLAUDE.md
git commit -m "feat(i18n): mensagem set_lang define o idioma da conexao"
```

---

## Task 4: Migrar a amostra do servidor (1 narração + 1 erro)

**Arquivos:**
- Modificar: `server.py:10502-10503` e `server.py:10513`, `server.py:10518`
- Modificar: `tools/test_idioma.py`

- [ ] **Passo 1: Escrever o teste que falha**

Acrescentar ao fim de `_rodar_verificacoes()` em `tools/test_idioma.py`:

```python
    print("\n[10] Varredura estática: toda chave usada em T(...) existe no dicionário")
    fonte = open(os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))),
                              "server.py"), encoding="utf-8").read()
    usadas = set(re.findall(r'\bT\(\s*"([^"]+)"', fonte))
    faltando = sorted(k for k in usadas if k not in S.LANG_STRINGS)
    check(f"nenhuma chave órfã em server.py (usadas: {len(usadas)})", not faltando)
    if faltando: print("     órfãs:", ", ".join(faltando))

    print("\n[11] Amostra migrada de verdade")
    check("a narração da porta usa T", 'T("narracao.abre_porta"' in fonte)
    check("o erro da porta usa T", 'T("erro.porta_longe")' in fonte)
```

- [ ] **Passo 2: Rodar o teste e confirmar que falha**

```bash
python tools/test_idioma.py
```

Esperado: as duas checagens da seção [11] falham (as frases ainda são strings cruas). A seção [10] passa à toa, porque ainda não há nenhum `T("...")` no `server.py` — é justamente por isso que ela imprime a contagem de chaves usadas.

- [ ] **Passo 3: Migrar a mensagem de erro**

Em `server.py`, dentro de `handle_open_door` (linhas 10502-10503), trocar:

```python
            await self.send_to(pid, {"type": "error",
                "msg": "Aproxime-se da porta para abri-la."})
```

por:

```python
            await self.send_to(pid, {"type": "error",
                "msg": T("erro.porta_longe")})
```

- [ ] **Passo 4: Migrar a narração (dois sites idênticos)**

Em `server.py`, nas linhas 10513 e 10518, trocar **as duas** ocorrências de:

```python
        await self.gm_say(f"🚪 **{p['name']}** abre uma porta!")
```

por:

```python
        await self.gm_say(T("narracao.abre_porta", nome=p["name"]))
```

Atenção à indentação: a da linha 10513 está dentro de um `if` (12 espaços) e a da 10518 está um nível acima (8 espaços). Preserve a indentação de cada uma.

- [ ] **Passo 5: Rodar o teste e confirmar que passa**

```bash
python tools/test_idioma.py
```

Esperado: `30 passaram, 0 falharam`, com a seção [10] reportando `usadas: 2`.

- [ ] **Passo 6: Commit**

```bash
git add server.py tools/test_idioma.py
git commit -m "feat(i18n): migra a amostra do servidor (narracao e erro da porta)"
```

---

## Task 5: Motor de tradução do cliente

**Arquivos:**
- Criar: `src/i18n.js`
- Criar: `tools/test_idioma_cliente.js`
- Modificar: `index.html:14`

- [ ] **Passo 1: Escrever o teste que falha**

Criar `tools/test_idioma_cliente.js`:

```js
// Motor de idioma do cliente — roda da raiz: node tools/test_idioma_cliente.js
const fs = require("fs");
const path = require("path");
const raiz = path.join(__dirname, "..");

let PASS = 0, FAIL = 0;
const check = (name, cond) => { if (cond) { PASS++; console.log("  ✅ " + name); }
                                else { FAIL++; console.log("  ❌ " + name); } };

// O dicionário e o motor são <script> no navegador: aqui simulamos o window.
global.window = {};
eval(fs.readFileSync(path.join(raiz, "src", "lang", "strings.js"), "utf8"));
eval(fs.readFileSync(path.join(raiz, "src", "i18n.js"), "utf8"));
const DICT = global.window.LANG_STRINGS;
const I18N = global.window.I18N;

console.log("\n[1] Dicionário");
check("window.LANG_STRINGS carregou", DICT && Object.keys(DICT).length > 0);
check("toda entrada tem pt", Object.values(DICT).every(e => typeof e.pt === "string"));

console.log("\n[2] t() — idioma, fallback e chave ausente");
check("padrão é português", I18N.lang === "pt");
check("t() em português", I18N.t("erro.porta_longe") === "Aproxime-se da porta para abri-la.");
I18N.setLang("en");
check("setLang troca o idioma", I18N.lang === "en");
check("t() em inglês", I18N.t("erro.porta_longe") === "Get closer to the door to open it.");
DICT["_teste.so_pt"] = { pt: "só em português" };
check("falta 'en' → cai no português", I18N.t("_teste.so_pt") === "só em português");
check("chave inexistente devolve a própria chave", I18N.t("_teste.nao_existe") === "_teste.nao_existe");
I18N.setLang("klingon");
check("idioma não suportado é ignorado", I18N.lang === "en");
delete DICT["_teste.so_pt"];

console.log("\n[3] Parâmetros {nome}");
check("substitui por nome",
      I18N.t("narracao.abre_porta", { nome: "Thorin" }) === "🚪 **Thorin** opens a door!");
check("parâmetro que falta fica visível",
      I18N.t("narracao.abre_porta") === "🚪 **{nome}** opens a door!");

console.log("\n[4] Callbacks de troca");
let avisos = [];
I18N.on(code => avisos.push(code));
I18N.setLang("pt");
check("callback recebe o novo idioma", avisos.length === 1 && avisos[0] === "pt");
I18N.setLang("pt");
check("trocar para o mesmo idioma não dispara de novo", avisos.length === 1);

console.log("\n[5] Chaves data-i18n do game.js existem no dicionário");
const gamejs = fs.readFileSync(path.join(raiz, "game.js"), "utf8");
const usadas = new Set();
for (const m of gamejs.matchAll(/data-i18n(?:-ph|-title)?="([^"]+)"/g)) usadas.add(m[1]);
const orfas = [...usadas].filter(k => !DICT[k]);
check(`nenhuma chave data-i18n órfã (usadas: ${usadas.size})`, orfas.length === 0);
if (orfas.length) console.log("     órfãs:", orfas.join(", "));

console.log("\n" + "=".repeat(62));
console.log(`  ${PASS} passaram, ${FAIL} falharam`);
console.log("=".repeat(62));
process.exit(FAIL ? 1 : 0);
```

- [ ] **Passo 2: Rodar o teste e confirmar que falha**

```bash
node tools/test_idioma_cliente.js
```

Esperado: `ENOENT: no such file or directory ... src\i18n.js`.

- [ ] **Passo 3: Implementar o motor do cliente**

Criar `src/i18n.js`:

```js
'use strict';
// ─── IDIOMA (i18n) — motor PURO do cliente ─────────────────────────────────
// Gêmeo do bloco de i18n do server.py: mesmo dicionário (src/lang/strings.js),
// mesma regra de fallback, mesma substituição de {parametro} por nome.
//
// Este módulo NÃO toca no DOM (regra do CLAUDE.md). Quem aplica na tela é o
// game.js, via _i18nApply(); quem persiste a escolha também é o game.js.
(function () {
  const DICT = (typeof window !== 'undefined' && window.LANG_STRINGS) || {};
  const PADRAO = 'pt';
  const SUPORTADOS = ['pt', 'en'];
  const PARAM = /\{(\w+)\}/g;

  let lang = PADRAO;
  const ouvintes = [];

  // Sem tradução no idioma pedido → português. Sem a chave → devolve a própria
  // chave: aparece na tela (fica óbvio o que falta traduzir) e nada quebra.
  function t(key, params) {
    const entry = DICT[key];
    if (!entry) {
      console.warn('[i18n] chave ausente:', key);
      return key;
    }
    let text = entry[lang] || entry[PADRAO] || key;
    if (params) {
      text = text.replace(PARAM, (m, k) => (k in params ? String(params[k]) : m));
    }
    return text;
  }

  function setLang(code) {
    if (SUPORTADOS.indexOf(code) < 0 || code === lang) return;
    lang = code;
    for (const fn of ouvintes) {
      try { fn(code); } catch (e) { console.error('[i18n] ouvinte falhou:', e); }
    }
  }

  window.I18N = {
    t: t,
    setLang: setLang,
    on: function (fn) { if (typeof fn === 'function') ouvintes.push(fn); },
    get lang() { return lang; },
    SUPORTADOS: SUPORTADOS,
    PADRAO: PADRAO,
  };
})();
```

- [ ] **Passo 4: Rodar o teste e confirmar que passa**

```bash
node tools/test_idioma_cliente.js
```

Esperado: `14 passaram, 0 falharam`, com a seção [5] reportando `usadas: 0` (ainda não marcamos nenhum HTML).

- [ ] **Passo 5: Carregar os dois arquivos no `index.html`**

Em `index.html`, substituir a linha 14:

```html
<script>(function(){var v=Date.now();document.write('<script src="src/visualConfig.js?v='+v+'"><\/script>');document.write('<script src="src/ui/theme.js?v='+v+'"><\/script>');})();</script>
```

por:

```html
<script>(function(){var v=Date.now();document.write('<script src="src/lang/strings.js?v='+v+'"><\/script>');document.write('<script src="src/i18n.js?v='+v+'"><\/script>');document.write('<script src="src/visualConfig.js?v='+v+'"><\/script>');document.write('<script src="src/ui/theme.js?v='+v+'"><\/script>');})();</script>
```

O dicionário e o motor carregam **antes** de tudo: `game.js` chama `t()` já na montagem do HTML. São `<script>` síncronos de propósito — nada de `fetch`, para não haver um instante de português antes do inglês e para funcionar mesmo se a página for aberta por `file://`.

- [ ] **Passo 6: Confirmar que o servidor serve os dois arquivos**

```bash
python -c "import server; print('src' in server._STATIC_ROOTS)"
```

Esperado: `True`. É por isso que o dicionário mora em `src/lang/` e não numa pasta `lang/` na raiz — a allow-list de estáticos é `('src', 'assets')`, e a raiz daria 404.

- [ ] **Passo 7: Commit**

```bash
git add src/i18n.js tools/test_idioma_cliente.js index.html
git commit -m "feat(i18n): motor de traducao do cliente e carregamento do dicionario"
```

---

## Task 6: Seletor 🌐 Idioma no painel ⚙️

**Arquivos:**
- Modificar: `game.js` (bloco `_audioPanelEnsure`, linhas 14920-14979; init na linha 15032)
- Modificar: `src/gameState.js` (`ws.onopen` nas linhas 1134 e 1157; export perto da linha 2528)
- Modificar: `src/lang/strings.js`

> **Lembrete:** edite `game.js` **apenas** com a ferramenta Edit. Nunca por PowerShell.

- [ ] **Passo 1: Confirmar que o atalho `t` não colide**

```bash
grep -nE "^(let|const|var|function) t\b" game.js
```

Esperado: **nenhuma saída**. Se houver, use `I18N.t(...)` no lugar do atalho `t(...)` em todo o restante do plano.

- [ ] **Passo 2: Acrescentar as chaves do painel ao dicionário**

Em `src/lang/strings.js`, dentro do objeto, acrescentar (antes do `}` final, com vírgula na entrada anterior):

```js
  "ui.menu.audio_title": { "pt": "Áudio, idioma e opções", "en": "Audio, language and options" },
  "ui.menu.musica":      { "pt": "🎵 Música", "en": "🎵 Music" },
  "ui.menu.sons":        { "pt": "🔊 Sons", "en": "🔊 Sounds" },
  "ui.menu.idioma":      { "pt": "🌐 Idioma", "en": "🌐 Language" },
  "ui.menu.voltar_inicio": { "pt": "⌂ Voltar ao menu inicial", "en": "⌂ Back to main menu" },
  "ui.menu.sair":        { "pt": "⏻ Sair do jogo", "en": "⏻ Quit game" },
  "ui.menu.timer_ativo":   { "pt": "⏳ Limite de turno: ATIVO — desativar",
                             "en": "⏳ Turn limit: ON — turn off" },
  "ui.menu.timer_inativo": { "pt": "⏳ Limite de turno: DESATIVADO — ativar",
                             "en": "⏳ Turn limit: OFF — turn on" },
  "ui.menu.timer_nota_host":  { "pt": "Vale para toda a partida.",
                                "en": "Applies to the whole game." },
  "ui.menu.timer_nota_outro": { "pt": "Apenas o anfitrião pode alterar esta opção.",
                                "en": "Only the host can change this option." },
  "ui.menu.timer_so_host":    { "pt": "Somente o anfitrião pode alterar o limite de turno.",
                                "en": "Only the host can change the turn limit." }
```

- [ ] **Passo 3: Implementar `setLang` no `gameState.js`**

Em `src/gameState.js`, logo **depois** da função `send` (que termina na linha 1036), inserir:

```js
  // ── Idioma ────────────────────────────────────────────────────────────────
  // O servidor traduz narração e erros no idioma de cada conexão; esta é a
  // única coisa que ele precisa saber. Guardado aqui para ser reenviado em toda
  // (re)conexão — inclusive no rejoin automático depois de uma queda.
  let myLang = 'pt';
  function setLang(code) {
    myLang = code;
    send({ type: 'set_lang', lang: code });
  }
```

Em `src/gameState.js`, na linha 1134 (`rejoinSaved`), trocar:

```js
    ws.onopen = () => send({ type: 'rejoin', code: s.code, name: s.name });
```

por:

```js
    ws.onopen = () => {
      send({ type: 'set_lang', lang: myLang });   // antes do rejoin: erros já chegam traduzidos
      send({ type: 'rejoin', code: s.code, name: s.name });
    };
```

Em `src/gameState.js`, na linha 1157 (`connect`), trocar:

```js
    ws.onopen = () => {
      if      (mode === 'create') send({ type: 'create_room', name });
```

por:

```js
    ws.onopen = () => {
      send({ type: 'set_lang', lang: myLang });   // antes de entrar: erros já chegam traduzidos
      if      (mode === 'create') send({ type: 'create_room', name });
```

Em `src/gameState.js`, no objeto exportado (perto da linha 2528, onde está `isPreview: PREVIEW,`), acrescentar:

```js
    setLang,
```

- [ ] **Passo 4: Implementar `_i18nApply` e a persistência no `game.js`**

Em `game.js`, logo **antes** de `// ── Painel ⚙️ de áudio (global, em todas as telas) ───` (linha 14920), inserir:

```js
// ── Idioma: persistência e aplicação no DOM ─────────────────────────────────
// O motor puro está em src/i18n.js. Aqui fica o que toca navegador e tela:
// gravar a escolha e reescrever os textos marcados com data-i18n.
const _LANG_KEY = 'lfh_lang';
const t = (chave, params) => I18N.t(chave, params);

function _langLoadPref(){
  try {
    const salvo = localStorage.getItem(_LANG_KEY);
    if (salvo) I18N.setLang(salvo);
  } catch (e) {}
}

// Só marque com data-i18n elementos cujo conteúdo INTEIRO é aquele texto —
// textContent apaga filhos (um <label> com <input> dentro perderia o input;
// nesses casos, envolva o texto num <span>).
function _i18nApply(root){
  const alvo = root || document.body;
  alvo.querySelectorAll('[data-i18n]').forEach(el => { el.textContent = t(el.dataset.i18n); });
  alvo.querySelectorAll('[data-i18n-ph]').forEach(el => { el.placeholder = t(el.dataset.i18nPh); });
  alvo.querySelectorAll('[data-i18n-title]').forEach(el => { el.title = t(el.dataset.i18nTitle); });
}

function _setLang(code){
  I18N.setLang(code);
  try { localStorage.setItem(_LANG_KEY, code); } catch (e) {}
  _i18nApply(document.body);
  _refreshTurnTimerOption();          // rótulos montados em JS, não por data-i18n
  // O servidor responde ao set_lang reenviando o estado, e esse reenvio cai no
  // caminho normal de render (GS.on('gameState')) — o log de narração reaparece
  // traduzido sem precisar de nenhuma função de re-render nova aqui.
  GS.setLang(code);
}
window._setLang = _setLang;
```

- [ ] **Passo 5: Acrescentar a linha do seletor ao painel**

Em `game.js`, dentro de `_audioPanelEnsure`, trocar o trecho que monta o botão e o topo do popup (linhas 14929-14937):

```js
    '<button id="audio-gear" title="Áudio (música e sons)" style="width:42px;height:42px;'
    + 'border-radius:50%;border:1px solid #2a4a2a;background:rgba(13,26,13,.82);color:#a0e0a0;'
    + 'font-size:20px;cursor:pointer;line-height:1;padding:0;box-shadow:0 2px 8px rgba(0,0,0,.5);">⚙️</button>'
    + '<div id="audio-pop" style="display:none;position:absolute;right:0;top:50px;width:210px;'
    + 'background:rgba(13,20,13,.96);border:1px solid #2a4a2a;border-radius:10px;padding:12px 14px;'
    + 'box-shadow:0 6px 20px rgba(0,0,0,.6);color:#cfe9cf;font-size:.8rem;">'
    +   '<div style="display:flex;justify-content:space-between;margin-bottom:5px;">'
    +     '<span>🎵 Música</span><span id="aud-music-val">' + pct(_mmVol) + '%</span></div>'
    +   '<input id="aud-music" type="range" min="0" max="100" value="' + pct(_mmVol) + '" style="width:100%;">'
```

por:

```js
    '<button id="audio-gear" data-i18n-title="ui.menu.audio_title" title="Áudio, idioma e opções" style="width:42px;height:42px;'
    + 'border-radius:50%;border:1px solid #2a4a2a;background:rgba(13,26,13,.82);color:#a0e0a0;'
    + 'font-size:20px;cursor:pointer;line-height:1;padding:0;box-shadow:0 2px 8px rgba(0,0,0,.5);">⚙️</button>'
    + '<div id="audio-pop" style="display:none;position:absolute;right:0;top:50px;width:210px;'
    + 'background:rgba(13,20,13,.96);border:1px solid #2a4a2a;border-radius:10px;padding:12px 14px;'
    + 'box-shadow:0 6px 20px rgba(0,0,0,.6);color:#cfe9cf;font-size:.8rem;">'
    // Idioma vem primeiro: é a opção que muda todo o resto do painel.
    +   '<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px;">'
    +     '<span data-i18n="ui.menu.idioma">🌐 Idioma</span>'
    +     '<select id="cfg-lang" style="background:rgba(13,26,13,.9);color:#cfe9cf;border:1px solid #2a4a2a;'
    +       'border-radius:5px;padding:3px 5px;font-family:inherit;font-size:.78rem;cursor:pointer;">'
    // Nomes de idioma ficam SEMPRE no próprio idioma — nunca traduzidos.
    +       '<option value="pt">Português</option><option value="en">English</option>'
    +     '</select></div>'
    +   '<div style="display:flex;justify-content:space-between;margin-bottom:5px;">'
    +     '<span data-i18n="ui.menu.musica">🎵 Música</span><span id="aud-music-val">' + pct(_mmVol) + '%</span></div>'
    +   '<input id="aud-music" type="range" min="0" max="100" value="' + pct(_mmVol) + '" style="width:100%;">'
```

- [ ] **Passo 6: Marcar o rótulo de Sons e os botões de saída**

Em `game.js`, dentro do mesmo `_audioPanelEnsure`, trocar (linha 14939):

```js
    +     '<span>🔊 Sons</span><span id="aud-sfx-val">' + pct(_sfxVol) + '%</span></div>'
```

por:

```js
    +     '<span data-i18n="ui.menu.sons">🔊 Sons</span><span id="aud-sfx-val">' + pct(_sfxVol) + '%</span></div>'
```

E trocar os dois botões de saída (linhas 14947-14950):

```js
    +     '<button id="cfg-voltar-inicio" style="width:100%;padding:8px;margin-bottom:6px;background:rgba(40,32,10,.6);'
    +       'border:1px solid #c8a95155;border-radius:6px;color:#e8d9a8;font-family:inherit;font-size:.8rem;cursor:pointer;">⌂ Voltar ao menu inicial</button>'
    +     '<button id="cfg-sair" style="width:100%;padding:8px;background:rgba(60,20,20,.6);'
    +       'border:1px solid #a0505055;border-radius:6px;color:#e8b0a8;font-family:inherit;font-size:.8rem;cursor:pointer;">⏻ Sair do jogo</button>'
```

por:

```js
    +     '<button id="cfg-voltar-inicio" data-i18n="ui.menu.voltar_inicio" style="width:100%;padding:8px;margin-bottom:6px;background:rgba(40,32,10,.6);'
    +       'border:1px solid #c8a95155;border-radius:6px;color:#e8d9a8;font-family:inherit;font-size:.8rem;cursor:pointer;">⌂ Voltar ao menu inicial</button>'
    +     '<button id="cfg-sair" data-i18n="ui.menu.sair" style="width:100%;padding:8px;background:rgba(60,20,20,.6);'
    +       'border:1px solid #a0505055;border-radius:6px;color:#e8b0a8;font-family:inherit;font-size:.8rem;cursor:pointer;">⏻ Sair do jogo</button>'
```

- [ ] **Passo 7: Ligar o `<select>` e sincronizar o valor mostrado**

Em `game.js`, dentro de `_audioPanelEnsure`, logo **depois** da linha que liga o slider de SFX (linha 14977, `sSl.oninput = ...`) e **antes** do `return wrap;`, inserir:

```js
  const langSel = wrap.querySelector('#cfg-lang');
  langSel.value = I18N.lang;
  langSel.onchange = () => _setLang(langSel.value);
  // O painel é montado uma vez só e reaproveitado: aplica o idioma atual nele
  // agora, senão abrir o menu depois de trocar mostraria os rótulos antigos.
  _i18nApply(wrap);
```

- [ ] **Passo 8: Traduzir os rótulos do limite de turno (montados em JS)**

Em `game.js`, em `_refreshTurnTimerOption` (linhas 14988-14989), trocar:

```js
  btn.textContent=ativo?'⏳ Limite de turno: ATIVO — desativar':'⏳ Limite de turno: DESATIVADO — ativar';
  btn.disabled=!host; btn.style.opacity=host?'1':'.55'; note.textContent=host?'Vale para toda a partida.':'Apenas o anfitrião pode alterar esta opção.';
```

por:

```js
  btn.textContent=t(ativo?'ui.menu.timer_ativo':'ui.menu.timer_inativo');
  btn.disabled=!host; btn.style.opacity=host?'1':'.55'; note.textContent=t(host?'ui.menu.timer_nota_host':'ui.menu.timer_nota_outro');
```

E na linha 14970, trocar:

```js
      if(!state || state.host!==GS.myPid){ toast('Somente o anfitrião pode alterar o limite de turno.','var(--red)'); return; }
```

por:

```js
      if(!state || state.host!==GS.myPid){ toast(t('ui.menu.timer_so_host'),'var(--red)'); return; }
```

- [ ] **Passo 9: Carregar a preferência no boot**

Em `game.js`, no IIFE `_audioInit` (linha 15032), trocar:

```js
(function _audioInit(){
  _audioLoadPrefs();
  _audioPanelEnsure();
```

por:

```js
(function _audioInit(){
  _langLoadPref();       // antes do painel: ele já nasce no idioma salvo
  _audioLoadPrefs();
  _audioPanelEnsure();
  _i18nApply(document.body);
```

- [ ] **Passo 10: Rodar os testes**

```bash
node tools/test_idioma_cliente.js
```

Esperado: `14 passaram, 0 falharam`, agora com a seção [5] reportando `usadas: 6` e nenhuma órfã.

- [ ] **Passo 11: Commit**

```bash
git add game.js src/gameState.js src/lang/strings.js
git commit -m "feat(i18n): seletor de idioma no painel de configuracoes"
```

---

## Task 7: Marcar a amostra de HTML (telas inicial e Meus Jogos)

**Arquivos:**
- Modificar: `game.js:174-246` (template do `screen-connect` e do `screen-savegames`)
- Modificar: `src/lang/strings.js`

- [ ] **Passo 1: Acrescentar as chaves das duas telas ao dicionário**

Em `src/lang/strings.js`, acrescentar dentro do objeto:

```js
  "ui.connect.cover_hint":  { "pt": "Clique para começar", "en": "Click to start" },
  "ui.connect.subtitulo":   { "pt": "RPG de Tabuleiro Online — até 6 jogadores",
                              "en": "Online Tabletop RPG — up to 6 players" },
  "ui.connect.titulo":      { "pt": "Entrar na Aventura", "en": "Join the Adventure" },
  "ui.connect.nome_label":  { "pt": "Seu nome de herói", "en": "Your hero name" },
  "ui.connect.nome_ph":     { "pt": "Ex: Thorin", "en": "e.g. Thorin" },
  "ui.connect.servidor_label": { "pt": "Endereço do servidor", "en": "Server address" },
  "ui.connect.pin_label":   { "pt": "PIN (4 dígitos) — para jogos salvos",
                              "en": "PIN (4 digits) — for saved games" },
  "ui.connect.btn_conta":   { "pt": "🎲 Entrar com minha conta", "en": "🎲 Sign in with my account" },
  "ui.connect.ajuda_conta": { "pt": "Primeira vez? O apelido acima vira sua conta. Use o mesmo apelido + PIN para voltar aos seus jogos.",
                              "en": "First time? The nickname above becomes your account. Use the same nickname + PIN to get back to your games." },
  "ui.connect.div_rapido":  { "pt": "ou jogo rápido (sem salvar)", "en": "or quick game (no saving)" },
  "ui.connect.btn_criar":   { "pt": "⚔ Criar Nova Sala", "en": "⚔ Create New Room" },
  "ui.connect.div_ou":      { "pt": "ou", "en": "or" },
  "ui.connect.codigo_label": { "pt": "Código da sala", "en": "Room code" },
  "ui.connect.btn_entrar":  { "pt": "Entrar", "en": "Join" },
  "ui.connect.btn_reconectar": { "pt": "🔌 Reconectar à última partida",
                                 "en": "🔌 Reconnect to last game" },
  "ui.savegames.titulo":    { "pt": "Meus Jogos", "en": "My Games" },
  "ui.savegames.div_amigo": { "pt": "entrar no jogo de um amigo", "en": "join a friend's game" },
  "ui.savegames.codigo_label": { "pt": "Código da sala do amigo", "en": "Friend's room code" },
  "ui.savegames.btn_entrar": { "pt": "Entrar", "en": "Join" },
  "ui.savegames.ajuda":     { "pt": "Peça o código que aparece no topo do lobby de quem criou o jogo. Você escolhe seu personagem lá e ele fica vinculado à sua conta.",
                              "en": "Ask for the code shown at the top of the lobby of whoever created the game. You pick your character there and it gets linked to your account." },
  "ui.savegames.div_criar": { "pt": "criar novo", "en": "create new" },
  "ui.savegames.nome_label": { "pt": "Nome do jogo", "en": "Game name" },
  "ui.savegames.nome_ph":   { "pt": "Ex: A Sociedade do Anel", "en": "e.g. The Fellowship of the Ring" },
  "ui.savegames.campanha_label": { "pt": "Campanha", "en": "Campaign" },
  "ui.savegames.mestre_label": { "pt": "Este jogo terá um Mestre humano",
                                 "en": "This game will have a human Game Master" },
  "ui.savegames.btn_criar": { "pt": "➕ Criar jogo", "en": "➕ Create game" },
  "ui.savegames.btn_voltar": { "pt": "← Voltar", "en": "← Back" }
```

- [ ] **Passo 2: Marcar a tela inicial**

Em `game.js`, no template do `screen-connect` (linhas 177-216), aplicar as marcações abaixo. Cada troca é um Edit isolado:

```html
    <div id="cover-hint">Clique para começar</div>
```
→
```html
    <div id="cover-hint" data-i18n="ui.connect.cover_hint">Clique para começar</div>
```

```html
    <p>RPG de Tabuleiro Online — até 6 jogadores</p>
```
→
```html
    <p data-i18n="ui.connect.subtitulo">RPG de Tabuleiro Online — até 6 jogadores</p>
```

```html
    <h2>Entrar na Aventura</h2>
```
→
```html
    <h2 data-i18n="ui.connect.titulo">Entrar na Aventura</h2>
```

```html
      <label>Seu nome de herói</label>
      <input id="input-name" type="text" maxlength="20" placeholder="Ex: Thorin" value="">
```
→
```html
      <label data-i18n="ui.connect.nome_label">Seu nome de herói</label>
      <input id="input-name" type="text" maxlength="20" data-i18n-ph="ui.connect.nome_ph" placeholder="Ex: Thorin" value="">
```

```html
      <label>Endereço do servidor</label>
```
→
```html
      <label data-i18n="ui.connect.servidor_label">Endereço do servidor</label>
```

```html
      <label>PIN (4 dígitos) — para jogos salvos</label>
```
→
```html
      <label data-i18n="ui.connect.pin_label">PIN (4 dígitos) — para jogos salvos</label>
```

```html
    <button class="btn-primary" onclick="entrarComConta()">🎲 Entrar com minha conta</button>
```
→
```html
    <button class="btn-primary" data-i18n="ui.connect.btn_conta" onclick="entrarComConta()">🎲 Entrar com minha conta</button>
```

```html
    <div style="font-size:.7rem;color:#8ab88a;margin-top:4px;">
      Primeira vez? O apelido acima vira sua conta. Use o mesmo apelido + PIN para voltar aos seus jogos.
    </div>
    <div class="divider">ou jogo rápido (sem salvar)</div>
    <button class="btn-primary" onclick="createRoom()">⚔ Criar Nova Sala</button>
    <div class="divider">ou</div>
```
→
```html
    <div style="font-size:.7rem;color:#8ab88a;margin-top:4px;" data-i18n="ui.connect.ajuda_conta">
      Primeira vez? O apelido acima vira sua conta. Use o mesmo apelido + PIN para voltar aos seus jogos.
    </div>
    <div class="divider" data-i18n="ui.connect.div_rapido">ou jogo rápido (sem salvar)</div>
    <button class="btn-primary" data-i18n="ui.connect.btn_criar" onclick="createRoom()">⚔ Criar Nova Sala</button>
    <div class="divider" data-i18n="ui.connect.div_ou">ou</div>
```

```html
      <label>Código da sala</label>
      <div class="join-row">
        <input id="input-code" type="text" maxlength="4" placeholder="ABCD">
        <button class="btn-secondary btn-sm" onclick="joinRoom()">Entrar</button>
```
→
```html
      <label data-i18n="ui.connect.codigo_label">Código da sala</label>
      <div class="join-row">
        <input id="input-code" type="text" maxlength="4" placeholder="ABCD">
        <button class="btn-secondary btn-sm" data-i18n="ui.connect.btn_entrar" onclick="joinRoom()">Entrar</button>
```

```html
            onclick="rejoinSaved()">🔌 Reconectar à última partida</button>
```
→
```html
            data-i18n="ui.connect.btn_reconectar" onclick="rejoinSaved()">🔌 Reconectar à última partida</button>
```

> **Deixe o bloco `#hint-host` (`⚙️ Como jogar: ... iniciar.bat ...`) em português.** Ele mistura `<b>` e `<code>` dentro do texto, e `textContent` apagaria essa marcação. Traduzir markup interno é assunto da etapa 2, com uma variante `data-i18n-html`.

- [ ] **Passo 3: Marcar a tela Meus Jogos**

Em `game.js`, no template do `screen-savegames` (linhas 222-244):

```html
    <h2>Meus Jogos</h2>
```
→
```html
    <h2 data-i18n="ui.savegames.titulo">Meus Jogos</h2>
```

```html
    <div class="divider">entrar no jogo de um amigo</div>
    <div class="field"><label>Código da sala do amigo</label>
      <div class="join-row">
        <input id="sg-join-code" type="text" maxlength="4" placeholder="ABCD">
        <button class="btn-secondary btn-sm" onclick="entrarPorCodigoLogado()">Entrar</button>
      </div>
    </div>
    <div style="font-size:.7rem;color:#8ab88a;margin-top:2px;">
      Peça o código que aparece no topo do lobby de quem criou o jogo. Você escolhe seu
      personagem lá e ele fica vinculado à sua conta.
    </div>
    <div class="divider">criar novo</div>
    <div class="field"><label>Nome do jogo</label>
      <input id="sg-name" type="text" maxlength="40" placeholder="Ex: A Sociedade do Anel"></div>
    <div class="field"><label>Campanha</label>
      <select id="sg-campaign"></select></div>
    <label style="display:flex;gap:6px;align-items:center;font-size:.8rem;">
      <input id="sg-master" type="checkbox"> Este jogo terá um Mestre humano</label>
    <button class="btn-primary" onclick="criarJogoSalvo()">➕ Criar jogo</button>
    <button class="btn-secondary" onclick="showScreen('screen-connect')" style="margin-top:8px;">← Voltar</button>
```
→
```html
    <div class="divider" data-i18n="ui.savegames.div_amigo">entrar no jogo de um amigo</div>
    <div class="field"><label data-i18n="ui.savegames.codigo_label">Código da sala do amigo</label>
      <div class="join-row">
        <input id="sg-join-code" type="text" maxlength="4" placeholder="ABCD">
        <button class="btn-secondary btn-sm" data-i18n="ui.savegames.btn_entrar" onclick="entrarPorCodigoLogado()">Entrar</button>
      </div>
    </div>
    <div style="font-size:.7rem;color:#8ab88a;margin-top:2px;" data-i18n="ui.savegames.ajuda">
      Peça o código que aparece no topo do lobby de quem criou o jogo. Você escolhe seu
      personagem lá e ele fica vinculado à sua conta.
    </div>
    <div class="divider" data-i18n="ui.savegames.div_criar">criar novo</div>
    <div class="field"><label data-i18n="ui.savegames.nome_label">Nome do jogo</label>
      <input id="sg-name" type="text" maxlength="40" data-i18n-ph="ui.savegames.nome_ph" placeholder="Ex: A Sociedade do Anel"></div>
    <div class="field"><label data-i18n="ui.savegames.campanha_label">Campanha</label>
      <select id="sg-campaign"></select></div>
    <label style="display:flex;gap:6px;align-items:center;font-size:.8rem;">
      <input id="sg-master" type="checkbox"> <span data-i18n="ui.savegames.mestre_label">Este jogo terá um Mestre humano</span></label>
    <button class="btn-primary" data-i18n="ui.savegames.btn_criar" onclick="criarJogoSalvo()">➕ Criar jogo</button>
    <button class="btn-secondary" data-i18n="ui.savegames.btn_voltar" onclick="showScreen('screen-connect')" style="margin-top:8px;">← Voltar</button>
```

> **Repare no `<span>` do checkbox do Mestre.** O `<label>` contém o `<input type="checkbox">`; marcar o `<label>` com `data-i18n` faria `textContent` apagar o checkbox. O texto foi envolvido num `<span>` justamente por isso — a mesma armadilha vale para qualquer rótulo com filho.

- [ ] **Passo 4: Rodar o teste do cliente**

```bash
node tools/test_idioma_cliente.js
```

Esperado: `14 passaram, 0 falharam`, seção [5] reportando `usadas: 33` e nenhuma órfã.

- [ ] **Passo 5: Commit**

```bash
git add game.js src/lang/strings.js
git commit -m "feat(i18n): traduz a tela inicial e a de jogos salvos"
```

---

## Task 8: Verificação final

**Arquivos:**
- Modificar: `CLAUDE.md`

- [ ] **Passo 1: Rodar as duas suítes novas**

```bash
python tools/test_idioma.py && node tools/test_idioma_cliente.js
```

Esperado: `30 passaram, 0 falharam` e `14 passaram, 0 falharam`.

- [ ] **Passo 2: Rodar as suítes que a mudança em `broadcast`/`send_to` poderia ter quebrado**

```bash
python tools/test_modo_mestre.py && python tools/test_guilda.py && python tools/test_cenas_conversa.py && python tools/test_masmorra_sequenciada.py
```

Esperado: `0 falharam` em todas. Antes de tratar qualquer vermelha como regressão, leia a memória `testes-pre-existentes-quebrados.md` — a suíte está 100% verde desde 2026-08-04, então uma vermelha aqui é regressão de verdade.

- [ ] **Passo 3: Verificação no jogo, com dois navegadores**

Só um teste automatizado não prova que o menu funciona. Com o servidor rodando (`python server.py`, ou o `iniciar.bat`):

1. Abrir `http://localhost:8765/index.html`. Confirmar que a tela inicial está em **português**.
2. Abrir o painel ⚙️. Confirmar a linha **🌐 Idioma** com "Português" selecionado.
3. Trocar para **English**. Confirmar, **sem recarregar**, que o painel e a tela inicial ficam em inglês.
4. Recarregar a página (F5). Confirmar que continua em inglês — a escolha ficou salva.
5. Criar uma sala nesta aba (em inglês). Numa **segunda aba**, trocar o idioma para Português e entrar na mesma sala pelo código.
6. Começar a partida e **abrir uma porta**. Confirmar que a aba em inglês lê `🚪 **<nome>** opens a door!` e a aba em português lê `🚪 **<nome>** abre uma porta!` — a mesma narração, cada um no seu idioma.
7. Ainda na partida, tentar abrir uma porta **distante**. Confirmar que o erro chega no idioma daquela aba.
8. Abrir o console (F12) e confirmar que **não** há avisos `[i18n] chave ausente`.

Anote o resultado de cada item. Se algum falhar, corrija antes de seguir.

- [ ] **Passo 4: Documentar a feature no CLAUDE.md**

Em `CLAUDE.md`, acrescentar um bloco de citação ao fim da seção de estado do projeto (junto dos outros `>` que descrevem subsistemas):

```markdown
> **Idioma (i18n) — motor PT/EN:** o painel ⚙️ ganhou a linha **🌐 Idioma** (`<select>`
> Português/English) que troca o idioma **por jogador** — dois jogadores na mesma sala
> podem estar em idiomas diferentes. Dicionário ÚNICO em `src/lang/strings.js`
> (`{chave: {pt, en}}`), lido pelo cliente via `<script>` (síncrono, sem flash e sem
> depender de `fetch`) e pelo servidor via `json.loads` do objeto — mesmo padrão de
> `tools/editor_catalog.js`. Mora em `src/` porque a allow-list de estáticos é
> `("src","assets")`. **Cliente:** `src/i18n.js` é motor PURO (`I18N.t/setLang/on`, zero
> DOM); `game.js` tem o atalho `t()`, a persistência (`lfh_lang` no localStorage) e
> `_i18nApply(root)`, que varre `[data-i18n]`/`[data-i18n-ph]`/`[data-i18n-title]`.
> **Cuidado:** só marque com `data-i18n` elemento cujo conteúdo INTEIRO é o texto —
> `textContent` apaga filhos (por isso o rótulo do checkbox de Mestre em
> `screen-savegames` tem o texto num `<span>`). **Servidor:** `T("chave", **params)`
> marca "texto ainda não traduzido"; `broadcast`/`send_to`/`err` resolvem via o
> parâmetro `default` do `json.dumps`, no idioma da conexão, agrupando por idioma (um
> `json.dumps` por idioma presente na sala). Isso cobre de graça o `gm_log` dentro do
> `game_state` (que passa a guardar `T` — seguro, pois o log nunca é gravado em disco).
> **Regra de migração:** string crua continua string crua e sai em português para todos;
> migrar uma frase é envolvê-la em `T(...)` e acrescentar a chave. Sem `en` → cai no
> `pt`; sem a chave → devolve a própria chave. Idioma por conexão em `LANG_BY_PID`
> (dict de módulo, chaveado pelo `pid` do contador global `new_id()`), não na sala:
> vale antes de entrar em sala e cobre o Mestre, que sai de `self.players` no
> `start_game`; limpo no `finally` do `handler`. Ao receber `set_lang` o servidor
> reenvia o estado, e esse reenvio cai no caminho normal de render do cliente — não há
> função de re-render nova. **Amostra traduzida nesta etapa:** painel ⚙️,
> `screen-connect`, `screen-savegames`, a narração de abrir porta e o erro de porta
> distante. O resto (~546 `gm_say` + ~470 erros + a interface) é a etapa 2; o bloco
> `#hint-host` e o conteúdo autoral (masmorras, campanhas, falas de NPC, itens/monstros
> custom) estão FORA de escopo. Spec/plano em
> `docs/superpowers/{specs,plans}/2026-08-10-idioma-i18n*`. Testes:
> `tools/test_idioma.py` e `tools/test_idioma_cliente.js` (node) — os dois têm varredura
> estática que aponta chave órfã, e é ela que diz, na etapa 2, o que ainda falta.
```

- [ ] **Passo 5: Commit final**

```bash
git add CLAUDE.md
git commit -m "docs(i18n): documenta o motor de idioma no CLAUDE.md"
```

---

## Notas para a etapa 2 (tradução em massa)

Não faz parte deste plano, mas é o que o motor foi desenhado para servir:

- As duas varreduras estáticas (`[10]` no Python, `[5]` no node) apontam chave órfã a qualquer momento. Uma varredura **inversa** — listar as chaves com `pt` mas sem `en` — vira o placar de progresso da tradução.
- Migrar em lotes por assunto (combate, magias, loja, guilda…) mantém cada commit revisável.
- Nomes de item, monstro, habilidade e classe vêm de catálogos (`WEAPONS`, `MONSTER_DEFS`, `GUILD_CATALOG`, `GRIMORIO`) e provavelmente pedem uma convenção de chave derivada do id (`item.espada_longa.nome`), em vez de `T(...)` em cada uso.
- O bloco `#hint-host` precisa de uma variante `data-i18n-html` para preservar `<b>` e `<code>`.
